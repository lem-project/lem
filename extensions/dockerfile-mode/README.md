# Lem Dockerfile Major Mode

This lem major-mode support 
- tree-sitter
- lsp via docker-language-server

It has no keybindings predefined

It doesn't handle container management (starting/stopping container etc...)
